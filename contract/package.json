{
  "name": "eclipse-poll-contract",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "compile": "compact compile +0.31.0 src/eclipse_poll.compact src/managed/eclipse_poll",
    "build": "rm -rf dist && npm run compile && mkdir -p dist && cp -R src/managed dist/managed && npx tsc",
    "test": "node --test test/compile.test.ts",
    "build:ts": "mkdir -p dist && cp -R src/managed dist/managed && npx tsc"
  },
  "dependencies": {
    "@midnight-ntwrk/compact-runtime": "0.16.0",
    "@midnight-ntwrk/compact-js": "2.5.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}